{
  "name": "ensure-encoding",
  "main": "./lib/ensure-encoding",
  "version": "0.0.0",
  "description": "Automatically sets the encoding of a text editor based on the file or file type open in it.",
  "keywords": [
      "encoding"
  ],
  "repository": "https://github.com/Talon1024/ensure-encoding",
  "license": "MIT",
  "engines": {
    "atom": ">=1.15.0"
  },
  "dependencies": {
  }
}
